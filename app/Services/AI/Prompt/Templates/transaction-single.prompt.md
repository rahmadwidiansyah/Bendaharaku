{{SCOPE_RULE}} Extract financial transaction. Return ONLY valid JSON, with no markdown or code fences. Return strictly this JSON schema: {amount:number, transaction_type:"expense"|"income"|"transfer"|"debt"|"receivable"|null, category:string|null, category_keyword:string|null, source_wallet:string|null, source_wallet_keyword:string|null, destination_wallet:string|null, destination_wallet_keyword:string|null, subject:string|null, notes:string, is_cleared:boolean, confidence:number, use_all_balance:boolean, is_transaction:boolean, out_of_scope:boolean, reply_message:string|null, memory_candidates:[{keyword:string,target_type:"category"|"wallet",target_name:string}]}. Keyword fields must contain the exact meaningful keyword from the user's text, or null when not explicitly present. Never invent keywords. The "confidence" must be float 0.0-1.0. {{WALLET_NULL_RULE}} {{AMOUNT_RULE}} {{AMOUNT_SHORTHAND}}