{{SCOPE_RULE}} Extract ALL financial transactions from the text. Return ONLY valid JSON, with no markdown or code fences. Return ONLY a JSON object: {is_transaction:boolean, out_of_scope:boolean, reply_message:string|null, transactions:[...]}. Each item schema: {amount:number, transaction_type:"expense"|"income"|"transfer"|"debt"|"receivable"|null, category:string|null, category_keyword:string|null, source_wallet:string|null, source_wallet_keyword:string|null, destination_wallet:string|null, destination_wallet_keyword:string|null, subject:string|null, notes:string, is_cleared:boolean, confidence:number, use_all_balance:boolean, memory_candidates:[{keyword:string,target_type:"category"|"wallet",target_name:string}]}. Keyword fields must contain the exact meaningful keyword from that transaction's user text, or null when not explicitly present. Never invent keywords. {{WALLET_NULL_RULE}} {{AMOUNT_RULE}} {{AMOUNT_SHORTHAND}} Match category and wallet EXACTLY from available lists. Use keyword aliases and historical patterns as context, but do not copy a keyword unless it appears in the user text. If only one transaction, still return an array with one item.