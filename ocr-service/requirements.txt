# OCR Microservice — Python Dependencies
# Audit date: 2026-07-22
# Strategy: Stay on PaddleOCR 2.x (stable, backward-compatible API).
#           PaddleOCR 3.x has breaking API changes and requires paddlex.
#
# paddlepaddle 2.6.2: Last 2.x release, available on PyPI, supports Python 3.11
# paddleocr 2.10.0:   Last 2.x release, backward-compatible PaddleOCR() API
# numpy 1.26.4:       Compatible with paddlepaddle 2.6.x and opencv 4.10.x
# opencv-python-headless 4.10.0.84: CPU-only, no GUI deps needed in Docker

fastapi==0.115.10
uvicorn[standard]==0.30.6
python-multipart==0.0.18
paddleocr==2.10.0
paddlepaddle==2.6.2
Pillow==10.4.0
numpy==1.26.4
opencv-python-headless==4.10.0.84
