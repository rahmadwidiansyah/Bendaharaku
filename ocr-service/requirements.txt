# OCR Microservice — RapidOCR (lightweight) for 2GB server
# Strategy: RapidOCR ONNX Runtime — ~20MB model, ~150MB RAM, no PaddlePaddle 1.5GB
# Tesseract is primary (in Laravel app container), RapidOCR is fallback via this service
#
# rapidocr-onnxruntime: ONNX version, mobile PP-OCRv4 models lite
# onnxruntime: CPU provider
# Pillow, numpy, opencv-headless: minimal deps

fastapi==0.115.10
uvicorn[standard]==0.30.6
python-multipart==0.0.18
rapidocr-onnxruntime==1.3.9
onnxruntime==1.17.1
Pillow==10.4.0
numpy==1.26.4
opencv-python-headless==4.10.0.84
